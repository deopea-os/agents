# Local zsh config for the agents project.
# Source this file to get the `agents` CLI on your PATH:
#
#   source ./.zshrc
#
# To load it automatically in every new terminal, add that line to ~/.zshrc.

export PATH="./.venv/bin:$PATH"
